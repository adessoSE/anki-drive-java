sudo: required
dist: precise
addons:
  apt:
    packages:
    - libudev-dev

before_install:
    - npm install -g mkdirp
#  - apt-get update
#  - apt-get install -y libudev-dev

language: java
jdk:
  - oraclejdk8
