sudo: required
dist: trusty
addons:
  apt:
    packages:
    - libudev-dev

before_install:
  - apt-get update
  - apt-get install -y libudev-dev

install:
  - ./gradlew clean
  - rm -rf node_modules
  - npm install
  - ./gradlew build

language: java
jdk:
  - openjdk11
