sudo:
    - required
os:
    - osx
addons:
  apt:
    packages:
    - cmake
    - time
    - libudev-dev

before_install:
  - apt-get update
  - apt-get install -y libudev-dev

language: java
jdk:
  - oraclejdk8
