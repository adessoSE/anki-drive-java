sudo: required
dist: trusty
addons:
  apt:
    packages:
    - libudev-dev

before_install:
  - apt-get update
  - apt-get install -y libudev-dev
  - npm install --

install:
  - ./gradlew build

language: java
jdk:
  - openjdk8
