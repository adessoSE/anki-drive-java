sudo: required
dist: trusty
addons:
  apt:
    packages:
    - libudev-dev

before_install:
  - apt-get update
  - apt-get install -y libudev-dev
  - rm -rf build/node_modules/
  - ./gradlew clean

install:
  - ./gradlew build

language: java
jdk:
  - openjdk11
